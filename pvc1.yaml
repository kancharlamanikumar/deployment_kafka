apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kafka-storage1
spec:
  storageClassName: default-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
